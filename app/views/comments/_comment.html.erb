<ul class="row box" id="comment-<%= comment.id %>">
  <div>
    <%= link_to gravatar_for(comment.user, size: 50), comment.user %>
  </div>
  <div>
    <div class="user"><%= link_to comment.user.name, comment.user %></div>
		<div class="content">
			<%= comment.content %>
		</div>
		<div class="timestamp">
			Posted <%= time_ago_in_words(comment.created_at) %> ago.
		</div>
  </div>
</ul>