<% provide(:title, 'detail micropost' ) %>
  <div class="container">
    <div class="col-md-4 main-comment">
      <%= link_to gravatar_for(@micropost.user, size: 50), @micropost.user %>
      <h4><%= @micropost.content %></h4>
      <%= render 'shared/comment_form' %>
    </div>
    <div class="col-md-8">
      <%= render @comments %>
    </div>
  </div>