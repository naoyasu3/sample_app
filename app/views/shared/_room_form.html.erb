<%= form_for(@room) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
    <%= f.text_area :name, placeholder: "Compose new room name..." %>
</div>
<%= f.submit "作成", class: "btn btn-primary" %>
<% end %>